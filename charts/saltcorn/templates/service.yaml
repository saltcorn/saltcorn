apiVersion: v1
kind: Service
metadata:
  name: {{ include "saltcorn.fullname" . }}
  labels:
    {{- include "saltcorn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "saltcorn.selectorLabels" . | nindent 4 }}
