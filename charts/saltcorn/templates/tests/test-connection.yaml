apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "saltcorn.fullname" . }}-test-connection"
  labels:
    {{- include "saltcorn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "saltcorn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
