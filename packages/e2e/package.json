{
  "name": "@saltcorn/e2e",
  "version": "0.3.4",
  "description": "Pluggable nocode",
  "license": "MIT",
  "devDependencies": {
    "@saltcorn/data": "^0.3.4",
    "@saltcorn/server": "^0.3.4",
    "jest": "^25.1.0",
    "puppeteer": "^4.0.0"
  },
  "scripts": {
    "test": "echo run gotest instead",
    "gotest": "jest --runInBand"
  },
  "repository": "github:saltcorn/saltcorn",
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/plugin_packages/"
    ],
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/plugin_packages/"
    ]
  },
  "publishConfig": {
    "access": "public"
  }
}
