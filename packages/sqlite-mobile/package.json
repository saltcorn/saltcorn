{
  "name": "@saltcorn/sqlite-mobile",
  "version": "1.6.0-alpha.8",
  "description": "Saltcorn SQLite structure for the mobile app",
  "homepage": "https://saltcorn.com",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "tsc": "tsc -p tsconfig.json",
    "clean": "rm -rf ./dist/*"
  },
  "author": "Tom Nielsen",
  "license": "MIT",
  "main": "dist/index.js",
  "exports": {
    "./*": "./dist/*.js"
  },
  "typesVersions": {
    "*": {
      "*": [
        "dist/*"
      ]
    }
  },
  "files": [
    "dist/**/*",
    "!dist/tsconfig.ref.tsbuildinfo"
  ],
  "dependencies": {
    "@saltcorn/db-common": "1.6.0-alpha.8",
    "@capacitor/core": "^6.2.0",
    "@capacitor-community/sqlite": "^6.0.2"
  },
  "repository": "github:saltcorn/saltcorn",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^20.14.7",
    "typescript": "5.9.2"
  }
}