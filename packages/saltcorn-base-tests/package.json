{
  "name": "saltcorn-base-tests",
  "version": "0.0.1",
  "description": "tests for data and base-plugin",
  "license": "MIT",
  "dependencies": {
    "saltcorn-markup": "^0.0.1",
    "saltcorn-data": "^0.0.1",
    "saltcorn-base-plugin": "^0.0.1",
    "saltcorn-sbadmin2": "^0.0.1"
  },
  "devDependencies": {
    "jest": "^25.1.0"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
